export interface ConstructorInput {
  /** API identity key issued by Binance payment system */
  publicKey: string;
  /** API secret key issued by Binance Merchant dashboard. see https://merchant.binance.com/en/dashboard/developers/api */
  secretKey: string;
}
